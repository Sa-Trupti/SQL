create table employee(
emp_id int,
emp_name char(20),
emp_designation char(15),
emp_salary int
);

It will create a table name employee.

#For get to know what is in the table we hve to write the following command.
Select * from <Table-Name>;
select * from emplyoee;


create table student(
s_id int,
s_name char(15),
s_per float
);


insert into student
values(1,'Trupti', 77.56),
(2,'Rohit',88.45),
(3,'Deepti',65.90),
(4,'Ayush',94.78);

select * from student;
select s_id from student;
